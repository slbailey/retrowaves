# Retrowaves Tower Configuration
# Copy this file to /etc/retrowaves/tower.env and customize as needed
# Environment variables take precedence over this file

# HTTP server host (default: 0.0.0.0)
TOWER_HOST=0.0.0.0

# HTTP server port (default: 8005)
TOWER_PORT=8005

# MP3 encoding bitrate (default: 128k)
TOWER_BITRATE=128k

# Tone generator frequency in Hz (default: 440)
TOWER_TONE_FREQUENCY=440

# Read chunk size in bytes (default: 8192)
TOWER_READ_CHUNK_SIZE=8192

# Unix domain socket path (default: /var/run/retrowaves/pcm.sock)
TOWER_SOCKET_PATH=/var/run/retrowaves/pcm.sock

# Slow-client timeout in milliseconds (default: 250)
TOWER_CLIENT_TIMEOUT_MS=250

# Per-client buffer size in bytes (default: 65536)
TOWER_CLIENT_BUFFER_BYTES=65536

# Encoder backoff schedule in milliseconds, comma-separated (default: 1000,2000,4000,8000,10000)
TOWER_ENCODER_BACKOFF_MS=1000,2000,4000,8000,10000

# Encoder maximum restart attempts (default: 5)
TOWER_ENCODER_MAX_RESTARTS=5

# Silence MP3 path for FAILED state (default: None, Tower generates silence internally)
# TOWER_SILENCE_MP3_PATH=/path/to/silence.mp3

# Logging level (default: INFO)
# Valid values: DEBUG, INFO, WARNING, ERROR, CRITICAL
TOWER_LOG_LEVEL=INFO

# Test mode (default: unset/empty)
# Set to "1", "true", "yes", or "on" to enable test mode (reduces backoff delays)
# TOWER_TEST_MODE=
